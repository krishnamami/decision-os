# Decision OS — Python dependencies
# Python 3.11+

# ── Runtime — currently imported ──────────────────────────────
pydantic>=2.0,<3.0
PyYAML>=6.0

# ── Runtime — production backends ─────────────────────────────
# In-memory variants exist for dev / tests; these are needed
# only when running against real Redis / Postgres.
redis>=5.0
asyncpg>=0.29
python-dotenv>=1.0

# ── Runtime — API + UI ────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.30
anthropic>=0.30
pdfplumber>=0.11   # Tier-1 extraction (RA-EX-F); local PDF text, no AWS
reportlab>=4.0     # Exam-ready PDF export (CN-EX); server-side generation
structlog>=24.1
httpx>=0.27
jinja2>=3.1
python-multipart>=0.0.9
bcrypt>=3.2  # auth password hashing (core/auth)
pyjwt>=2.8  # JWT access tokens (core/auth)

# ── Dev / tests ───────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
